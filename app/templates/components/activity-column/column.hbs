{{#if events.isFulfilled}}
  <div class="ui segments box-shadow-none border-none">
    {{#each events as |event|}}
      {{events/event-card event=event class="ui segment padding-vs" ellipsis=true}}
    {{/each}}
  </div>
{{/if}}
{{yield}}